import { LoopdirectiveDirective } from './loopdirective.directive';

describe('LoopdirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new LoopdirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
